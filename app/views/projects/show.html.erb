<% @page_header = "Portfolio :: #{@project.title}" %>

<div class="row">
  <div class="col-md-6">
    <%= image_tag("#{@project.project_image.url}") %>
  </div>



  <div class="col-md-6">
    <h4><%= link_to "#{@project.title}", '#' %></h4>
    <p><%= simple_format @project.description %></p>
  </div>

</div>
